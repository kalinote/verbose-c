// P1-1 指针循环：for (p = arr; p < arr + n; p++)。

int main() {
    int arr[4] = {1, 2, 3, 4};
    int* p = arr;
    int total = 0;

    for (p = arr; p < arr + 4; p++) {
        total += *p;
    }

    if (total != 10) {
        return 1;
    }
    return 0;
}
