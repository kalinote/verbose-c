// 审计：P0-5 浮点自增自减与复合赋值

int main() {
    float f = 1.5;
    f += 2.0;
    f++;
    ++f;
    if (f < 4.4 || f > 4.6) {
        return 1;
    }
    f--;
    --f;
    if (f < 2.4 || f > 2.6) {
        return 2;
    }
    return 0;
}

main();
